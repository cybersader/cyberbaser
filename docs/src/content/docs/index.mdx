---
title: Cyberbaser
description: An interoperability layer for contributable, version-controlled knowledge bases. The private owner-alpha pipeline is implemented and completed its first real policy-activated Save to the live site; routine owner use is current, while Q09 remains open.
hideMeta: true
sidebar:
  hidden: true
---

import RecentPages from '../../components/RecentPages.astro';

<div class="cb-hero" role="img" aria-label="Cyberbaser is an interoperability layer for contributable, version-controlled knowledge bases">
<span class="cb-hero-eyebrow">v1 Build · one owner loop complete</span>
<h2 class="cb-hero-headline">The contributable wiki, owner-controlled.</h2>
<p class="cb-hero-lead"><span class="cb-rotate-group">A <span class="cb-rotate-chip"><span class="cb-rotate-wrap"><span class="cb-rotate"><span>knowledge</span><span>research</span><span>compliance</span><span>handbook</span><span>team</span><span>knowledge</span></span></span></span></span> wiki whose source stays yours.</p>
<p class="cb-hero-subhead">An owner-controlled change boundary around a version-controlled knowledge base. It decides what may publish, prepares exact byte-preserving changes, and keeps final approval with the owner. GitHub and Quartz are today's dogfood infrastructure; the account-free path is not shipped.</p>
<div class="cb-hero-actions"><a class="cb-hero-cta cb-hero-cta-primary" href="/cyberbaser/getting-started/direction/">Current direction →</a><a class="cb-hero-cta cb-hero-cta-secondary" href="/cyberbaser/getting-started/vision/">Read the vision</a><a class="cb-hero-cta cb-hero-cta-secondary" href="/cyberbaser/research/concierge-human-correction-pilot/">The pilot</a><a class="cb-hero-cta cb-hero-cta-secondary" href="/cyberbaser/design/architecture/">Architecture</a></div>

  <script is:inline>{`
    (function() {
      var wrap = document.querySelector('.cb-rotate-wrap');
      var rotate = document.querySelector('.cb-rotate');
      var words = document.querySelectorAll('.cb-rotate > span');
      if (!wrap || !rotate || !words.length) return;
      // The container is whatever block-level element the wrap lives inside
      // (the hero lead paragraph). Use it for prefix/suffix measurement and
      // as the target of .cb-headline-stacked (renamed .cb-lead-stacked).
      var headline = wrap.parentElement;
      if (!headline) return;

      // words[last] is a duplicate of words[0] so the scroll animation has
      // a smooth target before snapping back. Only cycle through 0..n-1.
      var n = words.length - 1;
      var i = 0;

      // Helper: build a hidden measurement span that inherits headline font
      function makeTest() {
        var t = document.createElement('span');
        t.style.cssText = 'position:absolute;visibility:hidden;white-space:nowrap;font:inherit;';
        return t;
      }

      // Measure the widest word (used as the locked wrap width — no more
      // width animation per tick means no layout shifts).
      function measureMaxWord() {
        var test = makeTest();
        test.className = wrap.className; // inherit .cb-rotate-wrap font/gradient
        headline.appendChild(test);
        var max = 0;
        for (var j = 0; j <= n; j++) {
          test.textContent = words[j].textContent;
          var w = test.getBoundingClientRect().width;
          if (w > max) max = w;
        }
        headline.removeChild(test);
        return Math.ceil(max) + 8; // fudge for kerning + rounding
      }

      // Lock the wrap width to the widest word so tick() never has to
      // change width and the line never reflows within the group. Natural
      // text wrapping (plus white-space:nowrap on .cb-rotate-group)
      // handles layout at narrow viewports: the group stays glued as a
      // unit and breaks as a whole when there's no room.
      function applyLayout() {
        var maxW = measureMaxWord();
        wrap.style.width = maxW + 'px';
      }

      // Rotate one word forward. Pure transform — no width change.
      function tick() {
        i = (i + 1) % (n + 1);
        rotate.style.transform = 'translateY(-' + (i * 1.15) + 'em)';
        if (i === n) {
          // Landed on the duplicate word — snap back to index 0 without
          // animation so the next tick moves smoothly forward again.
          setTimeout(function() {
            rotate.style.transition = 'none';
            rotate.style.transform = 'translateY(0)';
            i = 0;
            void rotate.offsetHeight; // force reflow
            rotate.style.transition = '';
          }, 550);
        }
      }

      function start() {
        requestAnimationFrame(function() {
          applyLayout();
          setInterval(tick, 2500);
        });
      }

      var resizeTimer = null;
      window.addEventListener('resize', function() {
        clearTimeout(resizeTimer);
        resizeTimer = setTimeout(applyLayout, 150);
      });

      if (document.fonts && document.fonts.ready) {
        document.fonts.ready.then(start);
      } else {
        window.addEventListener('load', start);
      }
    })();
  `}</script>

</div>

<h2 id="authoring-storyboard" class="cb-storyboard-title">Different interfaces, one owner-controlled boundary</h2>
<p class="cb-storyboard-lead">Cyberbaser does not prescribe one editor. Readers, trusted contributors, and the owner can work through interfaces suited to their jobs. External tools propose bounded changes; the owner's Markdown source and final decision stay authoritative.</p>

<section class="cb-storyboard cb-journey cb-journey-4" aria-labelledby="authoring-storyboard">
  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">R</span><span class="cb-jstep-t">Reader micro-correction</span><span class="cb-who cb-who-ours">Cyberbaser concept</span><span class="cb-chip cb-chip-info">unbuilt</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-mock cb-mock-sm cb-mock-fill">
        <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">kb.example/security/passwords</span><span class="cb-mock-live">live</span></div>
        <div class="cb-mock-body">
          <div class="cb-mock-h">Password rotation</div>
          <p class="cb-mock-txt">Rotate every <span class="cb-mock-hl cb-mock-hl-bad">90 days</span>, regardless of compromise.</p>
          <div class="cb-mock-edit"><span class="cb-mock-edit-txt">“every 90 days” selected</span><span class="cb-mock-save">Propose correction</span></div>
        </div>
      </div>
    </div>
    <p class="cb-jstep-cap">This is the intended lightweight reader experience, not a working UI. The current pilot instead uses a seven-field concierge study form, which is not the final interface.</p>
  </div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">T</span><span class="cb-jstep-t">Trusted rich authoring</span><span class="cb-who cb-who-commodity">external editor</span><span class="cb-chip cb-chip-info">unselected</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-mock cb-mock-sm cb-mock-fill cb-rich">
        <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">editor.example/zero-trust</span></div>
        <div class="cb-rich-toolbar" aria-hidden="true"><span class="cb-rich-tool cb-rich-tool-strong">B</span><span class="cb-rich-tool cb-rich-tool-em">I</span><span class="cb-rich-tool">H2</span><span class="cb-rich-tool">↗</span><span class="cb-rich-tool">❝</span></div>
        <div class="cb-mock-body cb-rich-canvas">
          <div class="cb-mock-h">Zero trust</div>
          <p class="cb-mock-txt">Treat every request as untrusted, then verify identity, device, and context.</p>
          <div class="cb-mock-callout"><span class="cb-mock-callout-i">i</span>Rendered callouts and links stay visible while writing.</div>
        </div>
        <div class="cb-rich-foot"><span class="cb-chip cb-chip-info">source-bound proposal</span><span class="cb-mock-save">Submit for review</span></div>
      </div>
    </div>
    <p class="cb-jstep-cap">A rich browser, WYSIWYG, or CMS-like tool can be swapped in through a deliberate adapter. It may model blocks internally, but it must emit exact proposals instead of reserializing the file.</p>
  </div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">M</span><span class="cb-jstep-t">Maintainer local authoring</span><span class="cb-who cb-who-yours">owner local</span><span class="cb-chip cb-chip-ok">working</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-ed cb-ed-fill">
        <div class="cb-ed-bar"><span class="cb-ed-tab">zero-trust.md<span class="cb-ed-dirty"></span></span><span class="cb-who cb-who-yours">local</span></div>
        <div class="cb-ed-code">
          <div class="cb-ed-line"><span class="cb-ed-n">8</span><span class="cb-ed-t cb-ed-dim">---</span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">9</span><span class="cb-ed-t"><span class="cb-ed-tok">aliases:</span> [Zero-trust model]</span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">10</span><span class="cb-ed-t cb-ed-dim">---</span></div>
          <div class="cb-ed-line cb-ed-hot"><span class="cb-ed-n">14</span><span class="cb-ed-t"><span class="cb-ed-tok">&gt; [!tip]</span> Verify every request<span class="cb-mock-caret"></span></span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">15</span><span class="cb-ed-t">See <span class="cb-ed-tok">&#91;&#91;Identity#Signals&#93;&#93;</span>.</span></div>
        </div>
        <div class="cb-ed-foot"><span class="cb-chip cb-chip-ok">authoritative source</span>direct local edit · no Cyberbaser editor required</div>
      </div>
    </div>
    <p class="cb-jstep-cap">The owner keeps writing directly in the Markdown tool they prefer. Cyberbaser must not replace that editor or force owner work through an external moderation form.</p>
  </div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">✓</span><span class="cb-jstep-t">Owner review</span><span class="cb-who cb-who-ours">Cyberbaser boundary</span><span class="cb-chip cb-chip-info">static pilot card</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-review cb-review-fill">
        <div class="cb-review-hd"><span class="cb-review-av">27</span><span class="cb-review-who">candidate <span class="cb-review-trust">base 8c21… pinned</span></span><span class="cb-chip cb-chip-warn">full review</span></div>
        <div class="cb-review-diff"><div class="cb-diff cb-diff-rm">- Rotate every 90 days.</div><div class="cb-diff cb-diff-add">+ Change after compromise or exposure.</div></div>
        <div class="cb-checks">
          <div class="cb-check cb-check-ok"><span class="cb-check-ic">✓</span><span class="cb-check-t">OFM integrity: clean</span><span class="cb-check-meta">1 splice</span></div>
          <div class="cb-check cb-check-ok"><span class="cb-check-ic">✓</span><span class="cb-check-t">Untouched bytes: identical</span><span class="cb-check-meta">exact</span></div>
          <div class="cb-check cb-check-ok"><span class="cb-check-ic">✓</span><span class="cb-check-t">Candidate-only link regressions: 0</span><span class="cb-check-meta">rendered</span></div>
        </div>
        <div class="cb-rich-foot"><span class="cb-chip cb-chip-warn">pending owner</span><span>decision recorded separately</span></div>
      </div>
    </div>
    <p class="cb-jstep-cap">Reader, rich-editor, forge, and agent proposals converge on this evidence boundary: pinned source, exact diff, integrity and trust evidence, and rendered comparison. The private pilot kit renders a static local card; the owner records the decision separately. A generalized review UI remains unbuilt.</p>
  </div>
</section>

<p class="cb-fig-note cb-storyboard-note"><span class="cb-chip cb-chip-mute">one authority</span><strong>Interfaces are replaceable; the source boundary is not.</strong> The inline reader control shown here is a target concept, not shipped UI. The actual pilot uses a seven-field study form, a static local evidence card, and a separate owner decision record; no generalized interactive review UI has shipped. Rich authoring is intended but unselected; the owner's local editor remains primary.</p>

---

## Jump in

<div class="cb-grid">
  <a class="cb-card" href="/cyberbaser/concepts/problem/">
    <span class="cb-card-label">01 · Problem</span>
    <span class="cb-card-title">Why existing Obsidian→web tools fall short</span>
    <span class="cb-card-desc">Publishing works. Contribution doesn't. The gap cyberbaser fills.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/concepts/ecosystem/">
    <span class="cb-card-label">02 · Ecosystem</span>
    <span class="cb-card-title">The landscape around the problem</span>
    <span class="cb-card-desc">Obsidian Publish, Quartz, Digital Garden, Decap, and the gap none of them fill.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/design/translation-layer/">
    <span class="cb-card-label">22 · Hard problem</span>
    <span class="cb-card-title">The translation layer</span>
    <span class="cb-card-desc">Replaceable renderers outward; exact, source-bound candidate changes inward. Untouched Markdown bytes never pass through a whole-file serializer.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/getting-started/principles/">
    <span class="cb-card-label">12 · Principles</span>
    <span class="cb-card-title">The rules that constrain every decision</span>
    <span class="cb-card-desc">The owner controls the source. Untouched bytes survive. Accounts are not the safety mechanism. Offered paths remain independent.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/getting-started/roadmap/">
    <span class="cb-card-label">20 · Roadmap</span>
    <span class="cb-card-title">v1 Build and the next evidence gate</span>
    <span class="cb-card-desc">The private owner-alpha pipeline completed its first real policy-activated Save. R23's URL continuity checker is implemented and locally exercised against all 931 current public URLs; live enforcement installation remains pending. Attribution, Q09, moderation enforcement, and the general maintainer model remain open.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/research/prior-art/">
    <span class="cb-card-label">04 · Prior art</span>
    <span class="cb-card-title">What others tried and learned</span>
    <span class="cb-card-desc">Obsidian Publish, Quartz, Digital Garden, Notion-as-wiki, awesome lists — the data points.</span>
  </a>
</div>

<RecentPages />

{/* Styles for .cb-hero, .cb-rotate-wrap, .cb-flow-svg, .cb-sv-* etc. live in
    src/styles/brand.css. MDX <style> blocks can't use :global() reliably,
    which is why this file previously rendered the flow SVG as a black mess. */}
