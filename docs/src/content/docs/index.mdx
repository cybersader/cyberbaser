---
title: Cyberbaser
description: An interoperability layer for contributable, version-controlled knowledge bases. The current v1 product is an owner-controlled change boundary around a source you own; GitHub and Quartz are dogfood infrastructure, and the account-free path is under a precommitted human pilot.
hideMeta: true
sidebar:
  hidden: true
---

<div class="cb-hero" role="img" aria-label="Cyberbaser is an interoperability layer for contributable, version-controlled knowledge bases">
<span class="cb-hero-eyebrow">v1 Build · concierge pilot next</span>
<h2 class="cb-hero-headline">The contributable wiki, owner-controlled.</h2>
<p class="cb-hero-lead"><span class="cb-rotate-group">A <span class="cb-rotate-chip"><span class="cb-rotate-wrap"><span class="cb-rotate"><span>knowledge</span><span>research</span><span>compliance</span><span>handbook</span><span>team</span><span>knowledge</span></span></span></span></span> wiki whose source stays yours.</p>
<p class="cb-hero-subhead">An owner-controlled change boundary around a version-controlled knowledge base. It decides what may publish, prepares exact byte-preserving changes, and keeps final approval with the owner. GitHub and Quartz are today's dogfood infrastructure; the account-free path is not shipped.</p>
<div class="cb-hero-actions"><a class="cb-hero-cta cb-hero-cta-primary" href="/cyberbaser/getting-started/direction/">Current direction →</a><a class="cb-hero-cta cb-hero-cta-secondary" href="/cyberbaser/getting-started/vision/">Read the vision</a><a class="cb-hero-cta cb-hero-cta-secondary" href="/cyberbaser/research/concierge-human-correction-pilot/">The pilot</a><a class="cb-hero-cta cb-hero-cta-secondary" href="/cyberbaser/design/architecture/">Architecture</a></div>

  <script is:inline>{`
    (function() {
      var wrap = document.querySelector('.cb-rotate-wrap');
      var rotate = document.querySelector('.cb-rotate');
      var words = document.querySelectorAll('.cb-rotate > span');
      if (!wrap || !rotate || !words.length) return;
      // The container is whatever block-level element the wrap lives inside
      // (the hero lead paragraph). Use it for prefix/suffix measurement and
      // as the target of .cb-headline-stacked (renamed .cb-lead-stacked).
      var headline = wrap.parentElement;
      if (!headline) return;

      // words[last] is a duplicate of words[0] so the scroll animation has
      // a smooth target before snapping back. Only cycle through 0..n-1.
      var n = words.length - 1;
      var i = 0;

      // Helper: build a hidden measurement span that inherits headline font
      function makeTest() {
        var t = document.createElement('span');
        t.style.cssText = 'position:absolute;visibility:hidden;white-space:nowrap;font:inherit;';
        return t;
      }

      // Measure the widest word (used as the locked wrap width — no more
      // width animation per tick means no layout shifts).
      function measureMaxWord() {
        var test = makeTest();
        test.className = wrap.className; // inherit .cb-rotate-wrap font/gradient
        headline.appendChild(test);
        var max = 0;
        for (var j = 0; j <= n; j++) {
          test.textContent = words[j].textContent;
          var w = test.getBoundingClientRect().width;
          if (w > max) max = w;
        }
        headline.removeChild(test);
        return Math.ceil(max) + 8; // fudge for kerning + rounding
      }

      // Lock the wrap width to the widest word so tick() never has to
      // change width and the line never reflows within the group. Natural
      // text wrapping (plus white-space:nowrap on .cb-rotate-group)
      // handles layout at narrow viewports: the group stays glued as a
      // unit and breaks as a whole when there's no room.
      function applyLayout() {
        var maxW = measureMaxWord();
        wrap.style.width = maxW + 'px';
      }

      // Rotate one word forward. Pure transform — no width change.
      function tick() {
        i = (i + 1) % (n + 1);
        rotate.style.transform = 'translateY(-' + (i * 1.15) + 'em)';
        if (i === n) {
          // Landed on the duplicate word — snap back to index 0 without
          // animation so the next tick moves smoothly forward again.
          setTimeout(function() {
            rotate.style.transition = 'none';
            rotate.style.transform = 'translateY(0)';
            i = 0;
            void rotate.offsetHeight; // force reflow
            rotate.style.transition = '';
          }, 550);
        }
      }

      function start() {
        requestAnimationFrame(function() {
          applyLayout();
          setInterval(tick, 2500);
        });
      }

      var resizeTimer = null;
      window.addEventListener('resize', function() {
        clearTimeout(resizeTimer);
        resizeTimer = setTimeout(applyLayout, 150);
      });

      if (document.fonts && document.fonts.ready) {
        document.fonts.ready.then(start);
      } else {
        window.addEventListener('load', start);
      }
    })();
  `}</script>

</div>

<div class="cb-stack-wrapper">

  <svg class="cb-stack-svg" viewBox="0 0 920 640" preserveAspectRatio="xMidYMid meet" role="img" aria-label="Current v1 cross-section: readers consume a Quartz-rendered wiki; an owner-controlled change boundary selects public content and validates exact changes around an owner-controlled vault. Obsidian and GitHub are current dogfood paths, while account-free intake is only a concierge experiment.">
    <defs>
      <linearGradient id="cbs-hot" x1="0" y1="0" x2="1" y2="0">
        <stop offset="0" stop-color="#8b94a3"/>
        <stop offset="0.5" stop-color="#10b981"/>
        <stop offset="1" stop-color="#8b94a3"/>
      </linearGradient>
      <linearGradient id="cbs-hot-fill" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0" stop-color="rgba(16,185,129,0.18)"/>
        <stop offset="1" stop-color="rgba(16,185,129,0.04)"/>
      </linearGradient>
      <filter id="cbs-glow">
        <feGaussianBlur stdDeviation="6" result="b"/>
        <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
      </filter>
      <marker id="cbs-arrow-up" viewBox="0 0 10 10" refX="5" refY="2" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M0,10 L5,0 L10,10 Z" fill="#10b981"/>
      </marker>
      <marker id="cbs-arrow-down" viewBox="0 0 10 10" refX="5" refY="8" markerWidth="6" markerHeight="6" orient="auto">
        <path d="M0,0 L5,10 L10,0 Z" fill="#8b94a3"/>
      </marker>
    </defs>

    {/* ═══ LAYER 1: READERS (consumers at top) ═══ */}
    <text class="cbs-rail-label" x="460" y="25" text-anchor="middle">CONSUMED BY</text>
    <g class="cbs-reader">
      <circle cx="300" cy="60" r="22"/>
      <text class="cbs-reader-icon" x="300" y="67" text-anchor="middle">🧑‍💻</text>
      <text class="cbs-reader-name" x="300" y="104" text-anchor="middle">practitioner</text>
    </g>
    <g class="cbs-reader">
      <circle cx="400" cy="60" r="22"/>
      <text class="cbs-reader-icon" x="400" y="67" text-anchor="middle">🎓</text>
      <text class="cbs-reader-name" x="400" y="104" text-anchor="middle">student</text>
    </g>
    <g class="cbs-reader">
      <circle cx="500" cy="60" r="22"/>
      <text class="cbs-reader-icon" x="500" y="67" text-anchor="middle">🔍</text>
      <text class="cbs-reader-name" x="500" y="104" text-anchor="middle">searcher</text>
    </g>
    <g class="cbs-reader">
      <circle cx="600" cy="60" r="22"/>
      <text class="cbs-reader-icon" x="600" y="67" text-anchor="middle">🤝</text>
      <text class="cbs-reader-name" x="600" y="104" text-anchor="middle">colleague</text>
    </g>

    {/* Arrow: readers → web wiki */}
    <path d="M460,115 L460,148" class="cbs-flow-up"/>
    <text class="cbs-flow-label" x="470" y="136">reads</text>

    {/* ═══ LAYER 2: WEB WIKI ═══ */}
    <g class="cbs-layer">
      <rect x="120" y="150" width="680" height="60" rx="10"/>
      <text class="cbs-layer-title" x="460" y="180" text-anchor="middle">PUBLISHED VIEW</text>
      <text class="cbs-layer-sub" x="460" y="198" text-anchor="middle">Quartz + GitHub Pages today · swappable renderer and host</text>
    </g>

    {/* Arrow: web wiki → translation */}
    <path d="M460,215 L460,248" class="cbs-flow-up"/>
    <text class="cbs-flow-label" x="470" y="236">rendered by</text>

    {/* ═══ LAYER 3: TRANSLATION LAYER (HOT SPOT) ═══ */}
    <g class="cbs-layer-hot" filter="url(#cbs-glow)">
      <rect x="80" y="250" width="760" height="90" rx="14" fill="url(#cbs-hot-fill)" stroke="url(#cbs-hot)" stroke-width="2.5"/>
      <text class="cbs-layer-title-hot" x="460" y="282" text-anchor="middle">OWNER-CONTROLLED CHANGE BOUNDARY</text>
      <text class="cbs-layer-sub" x="460" y="302" text-anchor="middle">publish selection · exact splice · OFM integrity · trust route</text>
      <text class="cbs-layer-sub-mini" x="460" y="322" text-anchor="middle">classifies and prepares changes; the owner remains the authority</text>
    </g>

    {/* Bidirectional arrows: boundary ↔ vault */}
    <path d="M440,345 L440,380" class="cbs-flow-up"/>
    <path d="M480,380 L480,345" class="cbs-flow-down"/>
    <text class="cbs-flow-label" x="496" y="368">publishes / proposes exact change</text>

    {/* ═══ LAYER 4: OWNER-CONTROLLED VAULT ═══ */}
    <g class="cbs-layer cbs-layer-vault">
      <rect x="120" y="382" width="680" height="78" rx="10"/>
      <text class="cbs-layer-title-vault" x="460" y="412" text-anchor="middle">OWNER-CONTROLLED VAULT</text>
      <text class="cbs-layer-sub" x="460" y="430" text-anchor="middle">plain Markdown · version history · GitHub dogfood today</text>
      <text class="cbs-layer-sub-mini" x="460" y="448" text-anchor="middle">the authoritative files remain yours</text>
    </g>

    {/* Arrows from 5 authoring surfaces up to the vault */}
    <path d="M130,530 L165,465" class="cbs-flow-up cbs-contrib-arrow"/>
    <path d="M305,530 L320,465" class="cbs-flow-up cbs-contrib-arrow"/>
    <path d="M475,530 L475,465" class="cbs-flow-up cbs-contrib-arrow cbs-contrib-hot"/>
    <path d="M645,530 L605,465" class="cbs-flow-up cbs-contrib-arrow"/>
    <path d="M810,530 L780,465" class="cbs-flow-up cbs-contrib-arrow"/>

    {/* ═══ LAYER 5: CHANGE ORIGINS AND STATUS ═══ */}
    <text class="cbs-rail-label" x="460" y="500" text-anchor="middle">CHANGE ORIGINS · CURRENT STATUS</text>

    <g class="cbs-surface">
      <rect x="40" y="530" width="180" height="84" rx="10"/>
      <text class="cbs-surf-title" x="130" y="554" text-anchor="middle">Owner local edit</text>
      <text class="cbs-surf-sub" x="130" y="575" text-anchor="middle">built · dogfood</text>
      <text class="cbs-surf-mini" x="130" y="596" text-anchor="middle">Obsidian today</text>
      <text class="cbs-surf-mini" x="130" y="610" text-anchor="middle">direct local workflow</text>
    </g>

    <g class="cbs-surface">
      <rect x="240" y="530" width="150" height="84" rx="10"/>
      <text class="cbs-surf-title" x="315" y="554" text-anchor="middle">Account-free intake</text>
      <text class="cbs-surf-sub" x="315" y="575" text-anchor="middle">pilot only</text>
      <text class="cbs-surf-mini" x="315" y="596" text-anchor="middle">human concierge</text>
      <text class="cbs-surf-mini" x="315" y="610" text-anchor="middle">not a product path</text>
    </g>

    <g class="cbs-surface cbs-surf-hot">
      <rect x="405" y="530" width="140" height="84" rx="10" fill="rgba(16,185,129,0.08)" stroke="#10b981" stroke-width="1.5"/>
      <text class="cbs-surf-title" x="475" y="554" text-anchor="middle">GitHub editor</text>
      <text class="cbs-surf-sub" x="475" y="575" text-anchor="middle">built · borrowed</text>
      <text class="cbs-surf-mini" x="475" y="596" text-anchor="middle">account required</text>
      <text class="cbs-surf-mini" x="475" y="610" text-anchor="middle">raw Markdown · PR</text>
    </g>

    <g class="cbs-surface">
      <rect x="560" y="530" width="150" height="84" rx="10"/>
      <text class="cbs-surf-title" x="635" y="554" text-anchor="middle">Other local editor</text>
      <text class="cbs-surf-sub" x="635" y="575" text-anchor="middle">compatible in principle</text>
      <text class="cbs-surf-mini" x="635" y="596" text-anchor="middle">plain Markdown</text>
      <text class="cbs-surf-mini" x="635" y="610" text-anchor="middle">no product adapter yet</text>
    </g>

    <g class="cbs-surface">
      <rect x="725" y="530" width="160" height="84" rx="10"/>
      <text class="cbs-surf-title" x="805" y="554" text-anchor="middle">Automation adapter</text>
      <text class="cbs-surf-sub" x="805" y="575" text-anchor="middle">future · bounded</text>
      <text class="cbs-surf-mini" x="805" y="596" text-anchor="middle">must use exact changes</text>
      <text class="cbs-surf-mini" x="805" y="610" text-anchor="middle">no automatic writer</text>
    </g>
  </svg>

  <p class="cb-stack-caption">The current v1 boundary sits between an owner-controlled vault and a swappable published view. <strong>Two change mechanisms are shipped in the dogfood stack</strong>: the owner's local workflow and GitHub's account-required editor. Account-free intake is a precommitted concierge experiment, and every future adapter must preserve untouched bytes and owner authority.</p>
</div>

---

## Jump in

<div class="cb-grid">
  <a class="cb-card" href="/cyberbaser/concepts/problem/">
    <span class="cb-card-label">01 · Problem</span>
    <span class="cb-card-title">Why existing Obsidian→web tools fall short</span>
    <span class="cb-card-desc">Publishing works. Contribution doesn't. The gap cyberbaser fills.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/concepts/ecosystem/">
    <span class="cb-card-label">02 · Ecosystem</span>
    <span class="cb-card-title">The landscape around the problem</span>
    <span class="cb-card-desc">Obsidian Publish, Quartz, Digital Garden, Decap, and the gap none of them fill.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/design/translation-layer/">
    <span class="cb-card-label">22 · Hard problem</span>
    <span class="cb-card-title">The translation layer</span>
    <span class="cb-card-desc">Obsidian syntax → web HTML without losing round-trip fidelity. This is where the project lives or dies.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/getting-started/principles/">
    <span class="cb-card-label">12 · Principles</span>
    <span class="cb-card-title">The rules that constrain every decision</span>
    <span class="cb-card-desc">The owner controls the source. Untouched bytes survive. Accounts are not the safety mechanism. Offered paths remain independent.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/getting-started/roadmap/">
    <span class="cb-card-label">20 · Roadmap</span>
    <span class="cb-card-title">v1 Build and the next evidence gate</span>
    <span class="cb-card-desc">The concierge pilot is immediate. Enforcement, attribution, stable URLs, and the general maintainer model remain open.</span>
  </a>

  <a class="cb-card" href="/cyberbaser/research/prior-art/">
    <span class="cb-card-label">04 · Prior art</span>
    <span class="cb-card-title">What others tried and learned</span>
    <span class="cb-card-desc">Obsidian Publish, Quartz, Digital Garden, Notion-as-wiki, awesome lists — the data points.</span>
  </a>
</div>

{/* Styles for .cb-hero, .cb-rotate-wrap, .cb-flow-svg, .cb-sv-* etc. live in
    src/styles/brand.css. MDX <style> blocks can't use :global() reliably,
    which is why this file previously rendered the flow SVG as a black mess. */}
