---
title: Roadmap
description: Named phases from vault-first research through community launch — each decision logged with rationale. Modeled on crosswalker's research roadmap pattern.
sidebar:
  order: 20
status: research
tags: [roadmap, planning]
---

Cyberbaser is an **interoperability layer for contributable, version-controlled knowledge bases**. The contributable-wiki capability is what that layer enables: the current build uses Obsidian (and other markdown surfaces) as a first-class authoring surface and a Git-backed vault as today's source of truth. The dogfood contribution path currently requires GitHub; the account-free product path has not shipped and is now under a bounded [concierge pilot](/cyberbaser/research/concierge-human-correction-pilot/). The roadmap reflects this: research and architecture come first, features are built on that foundation, and every significant decision is [logged with rationale](/cyberbaser/agent-context/zz-log/).

See the [vault vision mining](/cyberbaser/agent-context/zz-log/2026-04-13-vault-vision-mining/) for the original dream, and the [vault tech stack mining](/cyberbaser/agent-context/zz-log/2026-04-13-vault-tech-stack-mining/) for the 300+ curated links that seed the research below.

:::note[How this roadmap is maintained]
This page is the **living roadmap** — only active and future phases live here. Every significant decision gets a dated entry in the [decision log](/cyberbaser/agent-context/zz-log/). Link roadmap items to their log entries so the *why* is always reachable from the *what*.
:::

---

## Current phase: v1 Build (foundations closed 2026-07-25)

**If you're returning after a long gap, the arc so far in five sentences.** The research phase settled the identity (an interoperability layer, not a publishing tool), proved a masking layer that protects Obsidian syntax through a markdown parser, and wrote the foundations. An adversarial pass then [audited the plan itself](/cyberbaser/research/plan-critique/) and produced [three competing designs for what v1 is](/cyberbaser/research/v1-build-plan/). Rather than argue, we ran the deciding measurement: the round-trip pipeline against **all 1430 real vault files**, which returned 4.6% byte-identity and proved that *whole-file re-serialization can never preserve real files* — so no such step is allowed in any write path, ever. That locked the v1 shape ([R12](/cyberbaser/research/v1-build-plan/#gate-result-run-2026-07-25)): **byte-preservation by construction** (edits are raw-text splices), **validation as the product** (a detector for tools that corrupt markdown), and governance on top. The first artifact, the [`@cyberbaser/ofm` validator](https://github.com/cybersader/cyberbaser/tree/main/packages/ofm), is built and tested.

### What the research phase resolved (the decision record)

Every entry links to evidence. The full log lives in `.claude/41-QUESTIONS-RESOLVED.md`; the deep pages are in [Research](/cyberbaser/research/).

| Decision | Outcome | Where |
| --- | --- | --- |
| Identity | An interoperability layer for contributable, version-controlled knowledge bases | [Assumptions & risks](/cyberbaser/research/assumptions-and-risks/) |
| Source of truth | Git stays, scoped, with an off-ramp; 83+ systems evaluated | [SSOT analysis](/cyberbaser/research/source-of-truth/) |
| Renderer | A commodity spoke, never coupled to the hub; two-spoke plan (Quartz for the vault, Starlight for these docs) | [Renderer + URLs](/cyberbaser/research/proposal-renderer-urls/) |
| Contribution policy | Trust curve + moderation queue; accounts are not the intended safety wall. The account-free product path remains unshipped and Q09 stays open | [Contribution workflows](/cyberbaser/design/contribution-workflows/) |
| Demand gate | Waived — the maintainer is user #1; dogfooding is the validation (R08) | [Plan critique](/cyberbaser/research/plan-critique/) |
| CMS | **None in the write path.** Every candidate re-serializes; the write path is raw-text only | [The write path](/cyberbaser/research/proposal-write-path/) |
| Plugin execution | Killed for v1 (0.9% of vault files need it; Tier-3 coupling) | [Plan critique](/cyberbaser/research/plan-critique/) |
| Real-time collab / RBAC / mixed-privacy | Out of cyberbaser — owned by a private sibling project (R10) | [Option space](/cyberbaser/research/option-space/) |
| The v1 shape | Byte-preservation + validator + governance, decided by measurement (R09→R12) | [v1 build plan](/cyberbaser/research/v1-build-plan/) |
| The Notion writer | Dormant since 2024-08; frozen and absorbed; restart only as a fenced bot (R13) | [Notion writer audit](/cyberbaser/research/notion-writer-audit/) |

### Building now

- [x] **Byte-preserving foundation** — `@cyberbaser/ofm` is a validator, never a writer; the publish boundary, projection, Quartz spoke, link checker, and trust classifier are built. The live vault proves the GitHub-backed dogfood path, not the general product.
- [ ] **Bounded concierge human-correction pilot — immediate.** Five ordinary readers and one independently operated Markdown-KB owner test whether a reader can submit a useful exact correction without an account and whether the owner can review and apply the one-splice candidate locally without GitHub. The protocol is precommitted; no attempts or results exist yet. [Read the thresholds and stop conditions](/cyberbaser/research/concierge-human-correction-pilot/).
- [ ] **Q09 remains open while the experiment runs.** The concierge is a study role, not a shipped intake endpoint, editor, hosted review console, automatic writer, or forge integration. Product automation is justified only if the pilot passes its reader-completion, anchoring, integrity, review-burden, owner-preference, and privacy thresholds.
- [ ] **Make enforcement honest without breaking daily authoring.** `ofm-check` remains report-only, the trust route remains decision-only, and direct maintainer pushes need a compatible validation path. See [Q10](/cyberbaser/reference/open-questions/#q10--what-enforces-the-moderation-policy).
- [ ] **Close the general-maintainer gap.** The dogfood stack still assumes GitHub and Actions. The pilot tests one local, non-GitHub review-and-apply workflow, but does not choose or ship the hosted, one-click self-hosted, or local-first operating model.
- [ ] **Freeze stable URLs.** Q06 and the slug-diff gate remain open for the live corpus.

The five-origin federation fixture remains **controlled local evidence only**. It does not create an interoperability claim or an immediate protocol workstream. Extracting and publishing a provisional profile waits until an independently operated peer demonstrates a concrete need to implement against it; the fixture can supply test material if that need appears. [Federation result and limits](/cyberbaser/research/federation/#measured-local-fixture-the-narrow-claim-passed).

### Reopened research (deleted in error 2026-07-25, restored 2026-07-26)

A knowledge-ops commit removed these open research tasks with no decision record. One of them, operational model and sustainability, was added by the maintainer deliberately (db015a2) and is a paraphrase of the question that triggered the 2026-07-26 alignment pass. They are open questions, not completed work.

- [ ] **Operational model + sustainability**: where does the sustained work live in cyberbaser? Which layer creates enough value that someone would pay to not build or maintain it? Candidate value centers: the translation/validation layer as a library, a contribution widget, plugin execution in CI, a hosted service, an enterprise tier. Informs license choice and what to protect versus give away. Intertwined with identity: "what is cyberbaser" and "what's the business" answer together. See [challenge 01](/cyberbaser/agent-context/zz-challenges/01-identity-operational-model/), only 2 of 6 deliverables ever produced.
- [ ] **Competitive counter-argument**: [challenge 01 item 5 and challenge 04 item 4](/cyberbaser/agent-context/zz-challenges/04-competitive-landscape/) both asked "what if Quartz + a CMS + GitHub Pages already covers 90% of this for free?" The project has since shipped approximately that configuration and never wrote the answer. The comparison table both briefs asked for does not exist.
- [ ] **Contributor attribution** ([challenge 07](/cyberbaser/agent-context/zz-challenges/07-contributor-attribution/)): falsely marked answered in v1-architecture. Attribution today is git history only. Reframed by the 2026-07-26 pass: this is not vanity, it is the **trust curve's missing input** — the curve promises trust driven by contribution history and nothing computes contributor history.
- [ ] **Publish site survey**: reader-experience patterns from published Obsidian sites (navigation, search, mobile, backlinks, attribution).
- [ ] **Data sync strategy**: media at scale, and the failure modes of content flowing between authoring surfaces.
- [ ] **SEO baseline**: partially delivered for free by the Quartz spoke (sitemap, RSS, OG images, alias redirects) and never recorded as delivered.

**Exit criteria for v1 Build:**

- [x] The vault renders and publishes through the publish boundary with leak tests green — **live 2026-07-25: [cybersader.github.io/cyberbase](https://cybersader.github.io/cyberbase/)** (R16: 933 pages + 373 assets, verbatim-path v0, deploy 16 s)
- [ ] The round-trip CI gate is a required check on the vault repo (report-only window running; its first rehearsal caught a masked crash on emoji paths, fixed — see R17)
- [ ] The maintainer makes a real web edit that lands as a clean splice-only commit — **machinery proven 2026-07-25/26**: two rehearsal edits ran the full circuit (queue → `ofm-check: clean` → merge → auto-redeploy → verified live), and every page now carries an "Edit this page" link. The criterion closes on the maintainer's own first click.
- [ ] The bounded human-correction pilot records five reader attempts and one independent owner's result against the precommitted thresholds; passing would justify only the smallest measured automation, not close Q09 by itself.
- [ ] URL contract frozen with the slug-diff gate on

---

## Publishing prototype <span class="cb-pill cb-pill-parked">Parked</span>

What exists from before the reinit (see [Existing Work](/cyberbaser/reference/existing-work/)):
- Astro + Starlight scaffold with Nova theme, full plugin stack
- 57 pages of first-principles content + vault mining logs
- Playwright smoke tests (32 passing)
- GitHub Pages deploy workflow (`actions/deploy-pages@v4`); Cloudflare reserved for edge-only CDN/WAF if ever needed
- Custom inline SVG diagrams, silver+emerald theme, rotating hero
- Cross-platform preflight script for WSL/Windows

Resumes after Research & Foundations with possible rewrites based on findings.

---

## Future phases <span class="cb-pill cb-pill-planning">Deferred</span>

:::note[Deferred by design]
Each gets a real outline only after the current phase's exit criteria are met. Roadmap is downstream of research, not the other way around.
:::

### Publish the real vault
Point at [cybersader/cyberbase](https://github.com/cybersader/cyberbase) through the publish boundary and deploy to a real domain. Depends on the boundary + URL freeze above. (The old "translation layer spike" phase was deleted by R12: forward rendering is bought from the commodity renderer, and the reverse direction needs no serializer because nothing re-serializes.)

### Contribution beyond the maintainer
Run the bounded [concierge correction pilot](/cyberbaser/research/concierge-human-correction-pilot/) first. If its precommitted thresholds pass, automate only the measured work around intake, exact source anchoring, and local review-card generation. The trust classifier already exists; an account-free endpoint, editor, automatic writer, and hosted moderation surface do not.

### Community onboarding
CONTRIBUTING.md, docs, examples, first external contributor.

---

## Operating rules

> [!important] Research = logged decision
> Every research task links to a dated entry in [Agent context · zz-log](/cyberbaser/agent-context/zz-log/). No research done off the books.

> [!important] Roadmap is downstream of research
> Don't add implementation steps to later phases until the relevant research task is resolved.

> [!important] Phase exits only by the exit criteria
> No "close enough" shortcuts.
