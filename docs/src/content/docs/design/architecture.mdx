---
title: Architecture
description: What cyberbaser is as built — five validator packages, three CI jobs, one pinned Quartz spoke, no server, and every human interface still borrowed from GitHub.
sidebar:
  order: 21
status: research
tags: [design, architecture]
---

{/*
  VISUAL CHOICE (2026-07-26, second pass): the layered box diagram that stood
  here for one day is gone. `.claude/FOCUS.md` line 17 locks the visual
  language — real-UI mockups, no abstract box diagrams — and 57 nodes in three
  colour-coded lanes told a reader where code lives without ever showing them
  what using this feels like.

  What replaced it: two journeys built from the shared illustration system in
  src/styles/brand.css (cb-journey, cb-mock, cb-ed, cb-review, cb-tree,
  cb-phone, cb-fed). The ownership information the old key carried now rides
  on the frames as cb-who chips, so nothing was lost except the boxes.

  No page-local <style> block: every class used here is in brand.css, where a
  second page can reuse it. Not Mermaid (rehype-mermaid shells out to a
  Playwright Chromium the deploy workflow never installs) and not one wide
  SVG (it scales down to ~5px text on a phone).
*/}

Cyberbaser, as built, is **five small libraries and three CI jobs that judge a markdown vault**: what may be published, whether an edit damaged the content, and who has to look at a change before it merges. It operates no server and runs entirely on someone else's runner. Every screen a human actually touches still belongs to GitHub or to Quartz.

## What a contribution looks like

<div class="cb-journey cb-journey-4" role="img" aria-label="Four screens in sequence. First, a published page with a typo and an edit-this-page pencil. Second, GitHub's plain-text editor with the line being fixed. Third, a review card showing the trust route, the diff and the machine checks. Fourth, the corrected page live again.">

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">1</span><span class="cb-jstep-t">A reader sees a typo</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-mock cb-mock-sm">
        <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">cybersader.github.io/cyberbase/…/zero-trust</span></div>
        <div class="cb-mock-body">
          <div class="cb-mock-h">Zero trust</div>
          <p class="cb-mock-txt">The perimeter model assumes everything is safe <span class="cb-mock-hl cb-mock-hl-bad">onece</span> you are inside the network.</p>
          <div class="cb-mock-edit"><span class="cb-mock-edit-txt">✎ Edit this page</span><span class="cb-who cb-who-ours">ours</span></div>
        </div>
      </div>
    </div>
    <p class="cb-jstep-cap">Everything on this screen is Quartz except the pencil. That anchor is <code>EditThisPage.tsx</code>: 87 lines of TSX that render one <code>&lt;a&gt;</code> at build time.</p>
  </div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">2</span><span class="cb-jstep-t">They fix it</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-ed">
        <div class="cb-ed-bar"><span class="cb-ed-tab">zero-trust.md<span class="cb-ed-dirty"></span></span><span class="cb-who cb-who-borrowed">GitHub</span></div>
        <div class="cb-ed-code">
          <div class="cb-ed-line"><span class="cb-ed-n">9</span><span class="cb-ed-t"><span class="cb-ed-tok">&gt; [!note]</span> Perimeter model</span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">10</span><span class="cb-ed-t">See <span class="cb-ed-tok">&#91;&#91;Segmentation&#93;&#93;</span> next.</span></div>
          <div class="cb-ed-line cb-ed-hot"><span class="cb-ed-n">11</span><span class="cb-ed-t">…safe once you are inside<span class="cb-mock-caret"></span></span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">12</span><span class="cb-ed-t cb-ed-dim">%%draft note, not published%%</span></div>
        </div>
        <div class="cb-ed-foot"><span class="cb-chip cb-chip-stop">account required</span>raw text · no preview</div>
      </div>
    </div>
    <p class="cb-jstep-cap">Today this is GitHub's plain-text box: wikilinks and callouts stay raw, nothing renders, and <strong>no account means no edit</strong> — which contradicts "accounts optional, never a wall".</p>
  </div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">3</span><span class="cb-jstep-t">Someone decides</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-review">
        <div class="cb-review-hd"><span class="cb-review-av">ak</span><span class="cb-review-who">a.kern <span class="cb-review-trust">first edit</span></span><span class="cb-chip cb-chip-warn">full-review</span></div>
        <div class="cb-review-diff"><div class="cb-diff cb-diff-rm">- safe onece you are</div><div class="cb-diff cb-diff-add">+ safe once you are</div></div>
        <div class="cb-checks">
          <div class="cb-check cb-check-ok"><span class="cb-check-ic">✓</span><span class="cb-check-t">ofm-check: clean</span><span class="cb-check-meta">advisory</span></div>
          <div class="cb-check cb-check-warn"><span class="cb-check-ic">!</span><span class="cb-check-t">anonymous author</span><span class="cb-check-meta">9 checks</span></div>
          <div class="cb-check cb-check-off"><span class="cb-check-ic">–</span><span class="cb-check-t">linkcheck: not run</span><span class="cb-check-meta">no workflow</span></div>
        </div>
        <div class="cb-review-actions"><span class="cb-review-btn cb-review-approve">Approve</span><span class="cb-review-btn cb-review-reject">Ask for changes</span></div>
      </div>
    </div>
    <p class="cb-jstep-cap">Every field here is already computed — the route, the fidelity verdict, the diff. <strong>Nothing renders it.</strong> The maintainer opens github.com, reads a raw diff and clicks Merge.</p>
  </div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">4</span><span class="cb-jstep-t">The page is fixed</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-mock cb-mock-sm">
        <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">cybersader.github.io/cyberbase/…/zero-trust</span><span class="cb-mock-live">● live</span></div>
        <div class="cb-mock-body">
          <div class="cb-mock-h">Zero trust</div>
          <p class="cb-mock-txt">The perimeter model assumes everything is safe <span class="cb-mock-hl">once</span> you are inside the network.</p>
          <div class="cb-mock-meta"><span class="cb-who cb-who-ours">publish + projection</span><span class="cb-who cb-who-commodity">Quartz</span><span class="cb-who cb-who-borrowed">Pages</span></div>
        </div>
      </div>
    </div>
    <p class="cb-jstep-cap">The merge fires one rebuild of all 933 pages: publish boundary, projection with its post-hoc leak test, Quartz, two gates, deploy. <strong>3m07s to 4m52s</strong>, measured over five runs.</p>
  </div>

</div>

<p class="cb-fig-note"><span class="cb-chip cb-chip-mute">honest</span>Two of these four screens are ours in name only. Screen 2 is GitHub's editor and screen 3 does not exist: its contents are printed into a CI step summary that is one click deep in the Actions tab.</p>

The step-3 card is the interesting one, because none of it is speculative work. `.cyberbaser/trust.yml` already sets the tiers, `@cyberbaser/trust` already runs nine mechanical checks and emits `auto-merge`, `quick-review`, `full-review` or `reject`, and `@cyberbaser/ofm` already classifies the change as `clean`, `suspect` or `damage`. The pipeline computes the whole card and then throws it at a log file. **The console's data layer is finished; the console is the missing 5%, not the missing 95%.**

<div class="cb-phone">
  <div class="cb-phone-notch" aria-hidden="true"></div>
  <div class="cb-phone-screen">
    <div class="cb-phone-status"><span>9:41</span><span>cyberbase</span></div>
    <div class="cb-phone-body">
      <div class="cb-phone-title">2 waiting on you</div>
      <div class="cb-queue">
        <div class="cb-queue-row"><span class="cb-review-av">ak</span><span class="cb-queue-t">fixes a typo in zero-trust</span><span class="cb-chip cb-chip-warn">full</span></div>
        <div class="cb-queue-row"><span class="cb-review-av">jd</span><span class="cb-queue-t">adds a link to segmentation</span><span class="cb-chip cb-chip-ok">quick</span></div>
      </div>
      <div class="cb-review-actions"><span class="cb-review-btn cb-review-approve">Approve</span><span class="cb-review-btn cb-review-reject">Later</span></div>
    </div>
  </div>
</div>

<p class="cb-fig-cap">The same two decisions, on a phone. This is what the computed data would look like if anything rendered it.</p>

<p class="cb-fig-note"><span class="cb-chip cb-chip-stop">not built</span>No queue, no auth, no server. Whether this should ever be a hosted screen or a static page generated in CI is the <a href="#open-the-console-fork">console fork</a>, still open and costed below.</p>

## What authoring looks like

<div class="cb-journey cb-journey-3" role="img" aria-label="Three screens. First, Obsidian editing a note with wikilinks and a callout. Second, a terminal pushing straight to main. Third, the rebuilt site live.">

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">1</span><span class="cb-jstep-t">Write in Obsidian</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-ed">
        <div class="cb-ed-bar"><span class="cb-ed-tab">zero-trust.md<span class="cb-ed-dirty"></span></span><span class="cb-who cb-who-commodity">Obsidian</span></div>
        <div class="cb-ed-code">
          <div class="cb-ed-line"><span class="cb-ed-n">1</span><span class="cb-ed-t cb-ed-dim">---</span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">2</span><span class="cb-ed-t"><span class="cb-ed-tok">publish:</span> true</span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">3</span><span class="cb-ed-t cb-ed-dim">---</span></div>
          <div class="cb-ed-line"><span class="cb-ed-n cb-ed-dim">⋯</span><span class="cb-ed-t cb-ed-dim"> </span></div>
          <div class="cb-ed-line cb-ed-hot"><span class="cb-ed-n">9</span><span class="cb-ed-t"><span class="cb-ed-tok">&gt; [!warning]</span> Trust nothing<span class="cb-mock-caret"></span></span></div>
          <div class="cb-ed-line"><span class="cb-ed-n">10</span><span class="cb-ed-t">See <span class="cb-ed-tok">&#91;&#91;Segmentation#Rules&#93;&#93;</span></span></div>
        </div>
        <div class="cb-ed-foot"><span class="cb-chip cb-chip-ok">graph · backlinks</span></div>
      </div>
    </div>
    <p class="cb-jstep-cap">Full Obsidian: wikilinks, callouts, block refs, the local graph. Cyberbaser is absent from this screen on purpose (<a href="/cyberbaser/getting-started/principles/">Principle 5</a>) and no plugin of ours is needed to open the vault.</p>
  </div>

  <div class="cb-jarr" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M3 12h14M13 6l6 6-6 6"/></svg></div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">2</span><span class="cb-jstep-t">Push</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-mock cb-mock-sm">
        <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">zsh — cyberbase</span><span class="cb-who cb-who-yours">yours</span></div>
        <div class="cb-term-body">
          <span class="cb-term-line">$ git commit -am "clarify perimeter"</span>
          <span class="cb-term-line">$ git push origin main</span>
          <span class="cb-term-line cb-term-dim">  To github.com:cybersader/cyberbase.git</span>
          <span class="cb-term-line cb-term-dim">  8f2c1ae..b41d9c0  main</span>
          <span class="cb-term-err">! no check ran on this push</span>
        </div>
      </div>
    </div>
    <p class="cb-jstep-cap">A direct push to <code>main</code>, usually through obsidian-git. <strong>Nothing validates it.</strong> Both validators are wired to <code>pull_request</code>, and the vault's <code>main</code> has no branch protection and no rulesets.</p>
  </div>

  <div class="cb-jarr" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="M3 12h14M13 6l6 6-6 6"/></svg></div>

  <div class="cb-jstep">
    <div class="cb-jstep-hd"><span class="cb-jstep-n">3</span><span class="cb-jstep-t">The site catches up</span></div>
    <div class="cb-jstep-frame">
      <div class="cb-mock cb-mock-sm">
        <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">…/cyberbase</span><span class="cb-mock-live">● live</span></div>
        <div class="cb-mock-body">
          <div class="cb-mock-h">Zero trust</div>
          <div class="cb-mock-callout"><span class="cb-mock-callout-i">!</span>Trust nothing</div>
          <p class="cb-mock-txt">See <span class="cb-linkchip">Segmentation</span> for the rules.</p>
        </div>
      </div>
    </div>
    <p class="cb-jstep-cap">All 933 pages rebuild through the same pipeline a contributor's merge fires, minus the two checks that never ran. Callout and wikilink survive the trip because the projection copies bytes and never re-serializes a file (<a href="/cyberbaser/research/v1-build-plan/#gate-result-run-2026-07-25">R12</a>).</p>
  </div>

</div>

<div class="cb-glance" role="img" aria-label="On the left, the vault as a folder of markdown files with two config files. On the right, the same note published as a page. The connector is labelled verbatim paths.">
  <figure class="cb-glance-panel">
    <div class="cb-tree cb-tree-mini">
      <div class="cb-tree-hd"><span class="cb-tree-fold">▾</span> cyberbase · main<span class="cb-tree-tag">yours</span></div>
      <div class="cb-tree-row cb-tree-hot"><svg viewBox="0 0 16 16" class="cb-tree-ic" aria-hidden="true"><path d="M4 1.8h4.2L12 5.4V14.2H4z"/><path d="M8.2 1.8V5.4H12"/></svg>zero-trust.md</div>
      <div class="cb-tree-row"><svg viewBox="0 0 16 16" class="cb-tree-ic" aria-hidden="true"><path d="M4 1.8h4.2L12 5.4V14.2H4z"/><path d="M8.2 1.8V5.4H12"/></svg>segmentation.md</div>
      <div class="cb-tree-row"><svg viewBox="0 0 16 16" class="cb-tree-ic" aria-hidden="true"><path d="M4 1.8h4.2L12 5.4V14.2H4z"/><path d="M8.2 1.8V5.4H12"/></svg>publish.yml<span class="cb-tree-tag">15 globs</span></div>
      <div class="cb-tree-row"><svg viewBox="0 0 16 16" class="cb-tree-ic" aria-hidden="true"><path d="M4 1.8h4.2L12 5.4V14.2H4z"/><path d="M8.2 1.8V5.4H12"/></svg>.cyberbaser/trust.yml<span class="cb-tree-tag">policy</span></div>
      <div class="cb-tree-hd cb-tree-sub"><span class="cb-tree-fold">▸</span> .obsidian/</div>
    </div>
    <figcaption class="cb-glance-cap">the source of truth · survives us</figcaption>
  </figure>
  <div class="cb-glance-link" aria-hidden="true">
    <span class="cb-glance-lab">projected</span>
    <span class="cb-flowline cb-flowline-r"></span>
    <span class="cb-glance-lab cb-glance-lab-g">verbatim paths</span>
  </div>
  <figure class="cb-glance-panel">
    <div class="cb-mock cb-mock-sm">
      <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">…/cyberbase/notes/zero-trust</span></div>
      <div class="cb-mock-body"><div class="cb-mock-h">Zero trust</div><div class="cb-mock-line"></div><div class="cb-mock-line cb-mock-line-85"></div></div>
    </div>
    <figcaption class="cb-glance-cap">rebuilt from scratch every push · disposable</figcaption>
  </figure>
</div>

<p class="cb-fig-cap">One file, two surfaces. Delete the right-hand side and nothing is lost; delete the left and everything is.</p>

:::danger[The maintainer's own path has fewer checks than a stranger's]
An honest typo that deletes a callout is caught in the contribution journey and missed in the authoring one. Both validators are wired to `pull_request` only, and direct pushes to `main` are not merely allowed, they are the maintainer's daily authoring path. That constraint is real (requiring PRs for everything would break Obsidian Git authoring), so the fix is not "turn on branch protection", it is **a `push`-triggered validation job that reports rather than blocks**. It has not been built.
:::

## The verdict

**Cyberbaser, as built, is five small libraries and three CI jobs that decide things about a markdown vault:** what may be published, whether a change damaged the content, and who has to look at a change before it merges. Every one of those capabilities is ours and tested; every one of them runs on someone else's runner, and **every human interface the system currently has belongs to GitHub or to Quartz.** The exceptions are one hyperlink and two terminal commands.

The gap is narrower than it looks. The pipeline already computes everything a maintainer console would show (which files crossed the publish boundary and which rule denied the rest, the fidelity verdict per changed file, the trust route and its reasons, the leak test, the artifact size against the host's cap) and emits all of it as structured JSON. **No human has ever seen any of it rendered.**

:::caution[What this page does not claim]
There is no CMS, no auth component, no moderation UI, no preview deployment, and no server. `main` on the vault repo has **no branch protection and no rulesets** (verified 2026-07-26: `branches/main/protection` returns 404, `rulesets` returns `[]`), `ofm-check.yml` is `continue-on-error: true`, and the auto-merge step in `trust-gate.yml` is commented out behind six named preconditions. **Nothing in this system can currently block a merge.** Everything below is written from the code, not from the plan.
:::

| As built, verified 2026-07-26 | |
|---|---|
| Packages | 5 (`ofm`, `publish`, `projection`, `linkcheck`, `trust`) |
| Tests green | 165 across the five packages, under `bun test` |
| CI workflows | 3 on the vault repo, 1 on this repo |
| Renderer spokes | 1 (Quartz v4.5.2, pinned, unforked), 0 tests |
| Pages live | 933 at [cybersader.github.io/cyberbase](https://cybersader.github.io/cyberbase/) |
| Servers cyberbaser operates | 0 |
| Human interfaces that are ours | 4 of 17 interaction points: one anchor tag, two CLIs, and this docs site |

**Where each part executes.** Four sites, and the fourth is empty.

| Execution site | What runs there |
|---|---|
| Reader's browser | Quartz's emitted HTML and JS. Nothing of ours runs client-side; our one component rendered to a static `<a>` at build time |
| CI runner (GitHub-hosted Ubuntu) | `publish`, `projection`, `ofm`, `trust`, Quartz, the Pages deploy |
| Developer machine | `linkcheck`, the four CLIs, this docs site |
| A cyberbaser-operated server | Nothing. There is no server |

The two lanes that run alongside the build are deliberately decoupled: `ofm-check` asks "is this change safe for Obsidian syntax", `trust-gate` asks "who has to look at it", and neither knows about the other. That decoupling is correct engineering and it is also why there is no single place a maintainer can look.

## Component reference

Two things worth knowing before the table. The renderer spoke has **zero tests** while the packages have 165, and `encodeRepoPath` in `EditThisPage.tsx` is exported specifically to be testable and has no test. And `agents: []` means the entire agent branch of the trust classifier, the most elaborate logic in that package, is unreachable in production today: every real PR takes the trusted-human branch or the terminal fallback.

<details>
<summary>Every package, deployable and config artifact — what it holds, where it runs, who owns it (14 rows)</summary>

Test counts are `bun test` runs on 2026-07-26.

| Component | What it does | Executes | State it holds | Tests | Ownership |
|---|---|---|---|---|---|
| [`@cyberbaser/ofm`](https://github.com/cybersader/cyberbaser/tree/main/packages/ofm) | Masks Obsidian constructs behind U+E000 sentinels so a CommonMark parser cannot reinterpret them; inventories 9 construct classes; classifies a change `clean \| suspect \| damage`; runs a corpus diagnostic | CI (`ofm-check.yml`) + dev CLI | None. Pure functions plus a report object | 10 | **Ours.** remark/unified underneath are commodity; the damage classifier is original |
| [`@cyberbaser/publish`](https://github.com/cybersader/cyberbaser/tree/main/packages/publish) | Default-deny publish boundary: deny flag → publish flag plus slug → allowlist → default deny → asset reachability. Extracts refs with code fences masked. Also carries the path safety lint and the slug contract | CI, through `projection` | None. Reads the vault, returns paths plus a report, never writes | 100 | **Ours.** No off-the-shelf equivalent for deny-wins plus reachability plus cross-boundary embeds |
| [`@cyberbaser/projection`](https://github.com/cybersader/cyberbaser/tree/main/packages/projection) | Runs the selector, pre-flights frontmatter, guards case collisions, lints path safety over the published set, copies to an output tree, then re-verifies the boundary against what landed. Writes `projection-report.json` | CI (`publish-site.yml`) | A disposable content tree plus the report | 28 | **Ours.** The post-hoc leak test (verify the copy, do not trust the plan) is the non-obvious part |
| [`@cyberbaser/linkcheck`](https://github.com/cybersader/cyberbaser/tree/main/packages/linkcheck) | Reads emitted HTML from any renderer, resolves every internal href against disk, sorts breaks into 4 classes with budgets. Zero dependencies | Dev machine only | None | 9 | **Ours, but replaceable.** This is `lychee --offline` with a per-class budget. First package to swap if maintenance gets tight |
| [`@cyberbaser/trust`](https://github.com/cybersader/cyberbaser/tree/main/packages/trust) | Reads `.cyberbaser/trust.yml`, derives a tier from author and author type, runs 9 mechanical checks, emits a route. Fails closed on a missing config | CI (`trust-gate.yml`) | None. Reads two files, prints a verdict | 18 | **Ours.** The individual checks are ordinary; the fail-closed tiering and the base-ref read are not |
| [`renderers/quartz-cyberbase`](https://github.com/cybersader/cyberbaser/tree/main/renderers/quartz-cyberbase) | `setup.sh` clones Quartz at the pinned tag and verifies it; `build.sh` symlinks a projected tree and builds. Config, layout and theme fill in Quartz's schema | CI + dev | None | **0** | **Ours (glue) over borrowed schema.** `EditThisPage.tsx` is the only UI element in the system we wrote; `RelativeFolderLinks.ts` fixes a real Quartz v4.5.2 link bug (248 broken attachment links to 7, measured) |
| Quartz v4.5.2 | The renderer: markdown to a static site, search, graph, explorer, backlinks | CI | The emitted site | n/a | **Commodity.** Pinned by tag, unforked, adopted on measurement ([R14](/cyberbaser/research/quartz-spike-results/)) |
| `publish-site.yml` (268 lines) | Checks out vault plus cyberbaser, projects, uploads the report, builds Quartz, runs two custom gates, deploys Pages. Refuses `pull_request_target` on purpose | GitHub Actions | The Pages deployment plus a 30-day report artifact | 0 | **Ours** (the job graph and the two gates) on **borrowed** actions |
| `ofm-check.yml` (70 lines) | Diffs changed markdown against the base blob and runs `ofm-check diff` per file; damage emits a warning annotation | GitHub Actions | PR annotations | 0 | **Ours.** `continue-on-error: true` |
| `trust-gate.yml` (208 lines) | Reads the policy from the base ref, builds a change description from the PR diff, runs `cb-trust`, writes a step summary and applies a label | GitHub Actions | A PR label plus a step summary | 0 | **Ours.** The forge adapter is a 60-line program inlined in a YAML heredoc, where it cannot be tested or reused |
| `publish.yml` (23 lines, in the vault) | 15 folder allow globs. The `audiences:` map the selector supports is unused | Read in CI | Publish policy | n/a | **Yours** (a file in your repo) |
| `.cyberbaser/trust.yml` (46 lines) | `trusted: [cybersader]`, `agents: []`, 5 caps, 15 allowed new folders, frontmatter allowlist `tags, updated` | Read in CI | Trust policy | n/a | **Yours.** Deliberately outside `.github/` so it stays forge-portable |
| This docs site (`docs/`) | Astro 6 plus Starlight 0.38 and 9 plugins, 56 content pages | CI (`deploy.yml`) + dev | Its own Pages deployment | 21 Playwright | **Commodity** stack; the content and `brand.css` are ours. Shares no code with the vault pipeline |
| GitHub | Identity, the editor, forks, PRs, review, merge, Actions, Pages, the audit trail | GitHub | Everything a human touches | n/a | **Borrowed interface** |

</details>

:::note[A gate that was documented before it ran]
`publish-site.yml` has always stated that `cb-project` runs "the path safety lint". Until the 2026-07-26 alignment pass that was false: `lintVault` / `lintPath` / `sluggify` / `slugKey` lived in `@cyberbaser/publish`, were exported from its index, and were called by nothing except their own tests, while the projection ran a narrower lint over frontmatter-derived paths only. The same pass wired the real lint in (`lintPathSafety`, `project.js`), so the comment is now true. Worth recording because it is the failure mode this page exists to prevent: a workflow comment described intent, the code did something narrower, and nobody noticed for a month.
:::

## The same two journeys, step by step

The illustrations above are the shape. These are the hand-offs, and the point of listing them is the ownership column: **steps 6, 7, 12, 13 and 14 are ours; steps 2, 3, 4, 5, 9, 10, 11 and 15 are GitHub's.** `linkcheck` appears nowhere, so the site can ship newly broken links and nothing notices.

<details>
<summary>Trace A: a contributor edits a live page (15 steps, measured 3m07s to 4m52s)</summary>

| # | Step | Component | Hand-off |
|---|---|---|---|
| 1 | Reader clicks "✎ Edit this page" on a published page | `EditThisPage.tsx`, a static anchor baked at build time | **Ours**, but it is only a link |
| 2 | Browser lands on GitHub's editor for that file path | GitHub | **Borrowed** |
| 3 | Contributor authenticates, or is stopped. No GitHub account, no edit | GitHub | **Borrowed.** This contradicts "accounts optional, never a wall" |
| 4 | Contributor types into a plain-text box. Raw `[[wikilinks]]`, `> [!note]` and `%%comments%%`, no rendering, no preview | GitHub | **Borrowed** |
| 5 | "Commit changes" forks the repo, creates a branch, opens a PR | GitHub | **Borrowed** |
| 6 | `ofm-check.yml` diffs each changed file against its base blob; damage emits a warning annotation | `@cyberbaser/ofm` | **Automated (ours).** Advisory: `continue-on-error: true` |
| 7 | `trust-gate.yml` runs in parallel, reads the policy from the base ref, classifies. An anonymous human draws `full-review` | `@cyberbaser/trust` | **Automated (ours).** Decision-only |
| 8 | A `trust:full-review` label appears, plus a table in the Actions step summary | `gh` in the workflow | **Ours, into a borrowed surface.** On a fork PR the token is read-only, so the label step often does nothing; the summary is one click deep |
| 9 | **The maintainer opens github.com, reads the raw diff, clicks Merge.** No rendered preview of the changed page exists anywhere in this flow | GitHub | **Manual and borrowed.** This is the step the docs used to claim was automated |
| 10 | Merge to `main` fires `publish-site.yml` | GitHub Actions | Borrowed trigger |
| 11 | Checkout vault (depth 1, no credentials) plus cyberbaser at `main` | `actions/checkout` | Borrowed |
| 12 | `cb-project`: selector, pre-flight, case-collision guard, path safety lint, copy, post-hoc leak test. Exits non-zero on a config error, unparseable frontmatter, a case collision or a boundary leak | `projection` + `publish` | **Automated (ours).** The first point in the loop that can fail the build, and it is already past the merge |
| 13 | `setup.sh` clones Quartz v4.5.2 and overlays our files; `build.sh` builds 933 pages | ours + Quartz | Automated, then **commodity** |
| 14 | Two custom gates: no `:` in an emitted path, artifact under the 1 GB cap | inline in the workflow | **Automated (ours)** |
| 15 | `upload-pages-artifact` then `deploy-pages`. Bytes live | GitHub | Borrowed |

</details>

<details>
<summary>Trace B: the maintainer pushes from Obsidian (5 steps, validated by nothing)</summary>

| # | Step | Component | Hand-off |
|---|---|---|---|
| 1 | Maintainer edits a note in Obsidian, with full wikilinks, callouts, graph and plugins | Obsidian | **Commodity.** Cyberbaser is absent by design |
| 2 | Commit and push, through obsidian-git or a terminal | Git | Commodity |
| 3 | With no branch protection this is a **direct push to `main`**. `ofm-check` and `trust-gate` are `pull_request`-triggered only | — | **Gap: validated by nothing** |
| 4 | `publish-site.yml` fires on push. Steps 11 to 15 of Trace A, identical | ours + GitHub | Automated |
| 5 | Site live in roughly 3 to 5 minutes | — | — |

</details>

## Boundaries: what each part must not do

The separations that keep the system honest, with how each one is currently held. "By code" means something fails if you violate it; "by convention" means nothing stops you.

| Part | Must **not** | Held by |
|---|---|---|
| **Vault (source of truth)** | hold build artifacts · depend on cyberbaser plugins to open in Obsidian · let its file layout be dictated by the URL scheme | **Convention.** R16 shipped verbatim paths partly to keep this true |
| **Publish boundary** | publish by default · trust the plan instead of the result · let a published page inline a private one | **Code.** Fail-closed config, post-hoc leak test, cross-boundary embed check, all fail the build |
| **Hub (the packages)** | apply lossy transforms · re-serialize a whole file in any write path · be coupled to one renderer · hold content state outside the commit it produces | **Code and decision.** [R12](/cyberbaser/research/v1-build-plan/#gate-result-run-2026-07-25) removed the re-serializing write path after measuring 4.6% byte-identity over 1,430 real files. Every package is a decider, not a writer |
| **Spoke renderer** | be treated as permanent or unique · hold contribution or auth state · be forked | **Convention, with one crack.** Quartz is pinned and unforked, but the folder-link fix landed as a Quartz plugin rather than in the projection. Worth watching |
| **Edit surfaces** | require Obsidian to be running · keep content outside the vault · make the maintainer do anything special per edit | **Borrowed.** GitHub satisfies all three today, at the cost of requiring an account |
| **Governance** | let a change promote its own author · decide from a policy the change itself supplied | **Code.** The policy is read from the base ref, and the classifier fails closed on unknown author types |

## Open architectural questions

The full list lives in [Open Questions](/cyberbaser/reference/open-questions/). These are the architecturally significant ones, with what is now settled marked as settled.

### Answered: where does the hub runtime live?

**It does not. The hub is libraries plus CI jobs.** Every capability runs either on a GitHub-hosted runner (`publish`, `projection`, `ofm`, `trust`, Quartz) or on a developer's machine (`linkcheck`, the CLIs). Nothing of ours runs in a reader's browser and nothing of ours runs on a server, because there is no server.

Exactly one component in the whole design would need one: **the anonymous write endpoint**, because accepting writes from someone with no account requires a credential the client cannot hold and a rate-limit ledger the client cannot keep. It is specified in [the hub bill of materials](/cyberbaser/research/proposal-hub-bom/) and **it is not built.** Its trigger is a real anonymous contributor, which has not happened.

This replaces the question that stood on this page from 2026-06-21 to 2026-07-26, when the honest answer was that nobody had checked.

### Open: the console fork

The system computes decisions no human sees — the step-3 card at the top of this page. Two designs were priced against a measured budget of roughly **4.7 maintainer-days per month** (96 commits across 11 calendar days in 219 days, tripled to allow for uncommitted effort).

| Option | Build | Ongoing | What it buys |
|---|---:|---:|---|
| **Hosted console** (auth, queue view, preview service, write-back, deploy, abuse handling) | **13.5 d** | **~1 d/month, forever** | One screen where the route, the fidelity verdict, the link delta and a render are the same object. Replayable policy: the trust classifier is pure, so any proposed setting can be re-run against real history |
| **Static generation plus CI annotation** (decision ledger, sticky PR comment, static status page, contributors page from git history, linkcheck on a second renderer) | **4.5 to 6.75 d** | ~0 | Satisfies every non-real-time promise in the vision without a runtime. Loses full-fidelity previews unless the 3-day artifact-preview pattern is also built |

The hosted option costs about three months of everything and then takes a permanent 21% of annual capacity. The static option is 2 to 3 times cheaper to build and has no maintenance tail. **Neither pass recommended a hosted application**, and the anti-console argument is strongest on the narrow claim: never ship a thing that holds moderation, identity or session state, because that is content state outside the commit it produces, which the boundaries table forbids.

The genuinely open part is the **decision ledger**: a `pull_request: closed` job appending one line per PR (route, verdict, checks, the maintainer's actual outcome) to a file in the vault. It is a day of work, it lives in a commit so it breaks no boundary, and it is the only path to the first of the six auto-merge preconditions ("at least 20 PRs classified with the maintainer's own decision recorded"). Nobody is keeping that record, so the agent lane can never be enabled on the project's own terms.

### Open: the missing enforcement

Every validator is advisory. Concretely: `ofm-check` is `continue-on-error`, `linkcheck` is in no workflow at all, the trust gate never merges and never blocks, and the vault's `main` has no protection and no rulesets. The projection is the single exception, and it only fails the build after a merge has already happened.

The constraint that shapes the fix: **the maintainer pushes to `main` directly from Obsidian Git**, so any proposal that requires PRs for all changes breaks daily authoring. The open question is which subset can become required without that collision, and the likely answer is a push-triggered reporting job plus required checks on PRs only.

### Open: 17 interaction points, 4 of them ours

Every place a human touches this system today, and whose product they are actually using.

| Whose UI | Count | Which interactions |
|---|---:|---|
| GitHub | 9 | Editing, authenticating, submitting, the OFM annotation, the trust label, the trust rationale, moderating and merging, downloading `projection-report.json`, build health |
| Quartz | 2 | Reading a page; search, graph, explorer, dark mode, backlinks, table of contents |
| A plain text editor | 2 | Hand-editing `publish.yml`; hand-editing `.cyberbaser/trust.yml`. R15 records that the maintainer does not hand-author YAML, so in practice this is an agent session |
| **Ours** | **4** | The "Edit this page" anchor (87 lines of TSX that render one `<a>`) · `cb-linkcheck` · `ofm-check` · this docs site |

Thirteen of the seventeen are not a cyberbaser surface, and the four that are consist of one hyperlink, two terminal commands, and a website about the project. Meanwhile `report.denied` (every file with the rule that denied it), `report.strippedLinks` (every boundary-crossing link with line numbers), `report.crossBoundaryEmbeds` and the full asset reachability chain all exist as JSON in a CI artifact nobody downloads.

### Open: assets and block IDs in a web edit

Reframed rather than answered. [R12](/cyberbaser/research/v1-build-plan/#gate-result-run-2026-07-25) removed the whole-file round-trip, so the question is no longer "how does the AST survive re-serialization" but "what does a raw-text splice do when the spliced region contains an embed or a block reference". Still the one genuinely unsolved Tier-1 item in the [translation layer](/cyberbaser/design/translation-layer/).

### Open: incremental builds, and what a second vault does to them

Partly measured, not solved. 933 pages rebuild fully in 3m07s to 4m52s in CI, and the artifact sits at roughly a quarter of the 1 GB Pages cap. Nothing incremental exists: every push rebuilds everything.

The question changes shape rather than answer once a second knowledge base is in the picture. Two vaults that link to each other are two independent sites, each with its own source of truth, discovering each other over a public manifest rather than sharing a database.

<div class="cb-fed" role="img" aria-label="Two browser windows side by side, each a separate wiki with its own vault. Between them, a two-way arrow labelled well-known manifest and harvest by commit hash.">
  <div class="cb-fed-site">
    <div class="cb-mock cb-mock-sm">
      <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">cybersader.github.io/cyberbase</span></div>
      <div class="cb-mock-body"><div class="cb-mock-h">Zero trust</div><p class="cb-mock-txt">See <span class="cb-linkchip">Segmentation</span> and <span class="cb-linkchip">Mesh policy ↗</span>.</p></div>
    </div>
    <span class="cb-fed-cap">your wiki · <span class="cb-fed-did">did:web:cybersader.github.io</span></span>
  </div>
  <div class="cb-fed-link" aria-hidden="true">
    <span class="cb-fed-lab">/.well-known/</span>
    <svg class="cb-fed-hop" viewBox="0 0 24 24"><path d="M4 9h16M4 9l3-3M4 9l3 3M20 15H4M20 15l-3-3M20 15l-3 3"/></svg>
    <span class="cb-fed-lab cb-fed-lab-g">harvest by commit</span>
  </div>
  <div class="cb-fed-site">
    <div class="cb-mock cb-mock-sm">
      <div class="cb-mock-bar"><span class="cb-mock-dot cb-mock-dot-r"></span><span class="cb-mock-dot cb-mock-dot-y"></span><span class="cb-mock-dot cb-mock-dot-g"></span><span class="cb-mock-url">notes.someone-else.org</span></div>
      <div class="cb-mock-body"><div class="cb-mock-h">Mesh policy</div><p class="cb-mock-txt">Their vault, their host, their moderation rules.</p></div>
    </div>
    <span class="cb-fed-cap">someone else's wiki · <span class="cb-fed-did">did:web:someone-else.org</span></span>
  </div>
</div>

<p class="cb-fig-note"><span class="cb-chip cb-chip-mute">researched, not built</span>Nothing in the pipeline knows about a second vault yet. The manifest-plus-harvest shape is written up in <a href="/cyberbaser/research/federation/">federation</a>; the cost that lands here is that a cross-vault link cannot be resolved by a build that only sees one repo.</p>

## Related

- [Contribution workflows](/cyberbaser/design/contribution-workflows/) — the same two journeys from the contributor's side, and the moderation policy
- [Principles](/cyberbaser/getting-started/principles/) — the six constraints every choice above is checked against
- [Translation layer](/cyberbaser/design/translation-layer/) — the tier system and what the OFM validator is protecting
- [Contribution on your terms](/cyberbaser/concepts/problem/#contribution-on-your-terms) — the trust dial that `.cyberbaser/trust.yml` implements
- [Federation](/cyberbaser/research/federation/) — what a second linked knowledge base costs this architecture
- [Quartz spike results](/cyberbaser/research/quartz-spike-results/) — why the renderer is Quartz and why it is pinned
- [v1 build plan](/cyberbaser/research/v1-build-plan/#gate-result-run-2026-07-25) — the 4.6% measurement that made every package a validator
- [Self-hosted Forgejo auth (RA-01)](/cyberbaser/design/reference-architectures/self-hosted-forgejo-auth/) — the PKCE path that removes the OAuth proxy if the forge ever moves
- [Roadmap](/cyberbaser/getting-started/roadmap/) — what is building now and what the exit criteria are
- Source: [cybersader/cyberbaser](https://github.com/cybersader/cyberbaser) · vault: [cybersader/cyberbase](https://github.com/cybersader/cyberbase) · live site: [cybersader.github.io/cyberbase](https://cybersader.github.io/cyberbase/)
- External: [Quartz v4.5.2](https://github.com/jackyzha0/quartz/releases/tag/v4.5.2) · [GitHub Security Lab on pwn requests](https://securitylab.github.com/resources/github-actions-preventing-pwn-requests/), the reason `publish-site.yml` refuses `pull_request_target`
