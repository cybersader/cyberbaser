# Cyberbaser — Current Focus

> Update when direction changes, milestones complete, or priorities shift.

**Current:** **v1 Build.** The current product interpretation is an **owner-controlled change boundary** around a version-controlled knowledge base: decide what may publish, accept bounded proposals against pinned source, preserve untouched bytes, classify integrity and trust, and leave final control with the owner. Authoring tools are replaceable spokes: reader controls, trusted rich browser/WYSIWYG/CMS-like tools, forge editors, local Markdown tools, and agents may present different experiences. External spokes converge on exact proposals and owner review; the owner keeps direct local authoring. GitHub, Actions, Pages, and pinned Quartz are the working dogfood stack, not the product essence. `OD-01` completed the one real owner loop required in this phase on 2026-07-31: signed-out phone handoff, immutable policy-bearing repin, isolated render, owner review and acceptance, separate exact source application, normal publication, and live verification. The manual series is now closed by supersession after that loop. The preserved charter remains historical evidence; `OD-02` and `OD-03` are **Not run — superseded**. Deterministic `ADV-*` scenarios cover stale source, ambiguous quote, and rejection-path binding as synthetic mechanical evidence only, and the tracked live verifier rechecks an already authorized commit, deployment run, and public page without writing source or mutating Git. `@cyberbaser/correction`, the private kit, review card, and seven-field instrument remain experiment infrastructure, not a shipped external editor, generalized adapter contract, or account-free product path. A separate private owner-local alpha is now the selected dogfood writer: one Save authorizes a strict durable policy to run the exact checks, application, commit, push, deployment observation, live verification, and local rebuild automatically. External spokes remain proposal-only. WP1's two-stage decision-ledger adapter is now implemented and hermetically tested: Stage A emits only a non-authoritative routing hint, while trusted Stage B reconstructs authority and publishes one ledger-only commit. It is not installed in the live vault, no real fork run occurred, and there are zero live observations; installation remains a separate explicit authorization boundary. The five-reader independent protocol is preserved for later if stronger usability claims need it.

Federation remains the long-term non-central seam. The five-origin fixture is controlled local compatibility only, not independent interoperability and not the next build workstream. Preserve it as evidence; extract a provisional profile only after an independently operated peer demonstrates a concrete need.

Last updated: 2026-08-03 (WP3 adapter and fixture implementation tested; real gate quarantined pending reviewed runner isolation; WP4 blocked; no new R-number)

## Locked This Cycle (do not relitigate without new evidence)

- **Identity / essence:** *an interoperability layer for contributable, version-controlled knowledge bases.* General, not cyber (cyberbase = dogfood). Category word: "layer." Never pitch as "publishing tool / CMS / Obsidian-to-web."
- **Owner-controlled boundary, hub-and-spoke:** the current v1 product is the change boundary around an owner-controlled vault: publish selection, pinned source/base identity, bounded exact changes, integrity/trust judgments, and owner review. Authoring and rendering tools are independently replaceable spokes. External authoring spokes propose; the owner-local editor writes the authority directly. Quartz and GitHub are current dogfood spokes. Federation is the long-term owner-controlled publication seam between independent bases and ordinary meta-wikis, **never a central Cyberbaser database or registry**. The local fixture is controlled evidence only; profile extraction waits for demonstrated peer need.
- **Contribution model:** maintainer-set **trust curve + moderation queue**; accounts never forced ("contributable, not controllable"); DoS handled at the edge, separate from content moderation.
- **Hosting/identity:** GitHub Pages current deploy target; Cloudflare edge-only; **self-hosted Forgejo preferred** (RA-01: PKCE against Forgejo OIDC, no OAuth proxy). No hyperscalers.
- **SSOT:** git, *scoped* — current manifestation, not essence, with an off-ramp (Principle 1 = "a single source of truth you own").
- **Write-path rule:** the 20/21 fixture spike proved masking through a parse, but the 1,430-file gate measured only 4.6% byte identity and disproved whole-file round-tripping. Cyberbaser-mediated source application uses exact, base-bound operations and preserves untouched bytes by construction; `@cyberbaser/ofm` validates and classifies damage. This constrains adapter output, not rich browser/WYSIWYG/CMS-shaped presentation.
- **Visual language for the docs:** real-UI mockups (browser windows, file trees, diffs, review cards) with tool logos; no abstract box diagrams; equal grids; topics varied and general.

## Project State

- **Current phase: v1 Build.** Research & Foundations closed on 2026-07-25; the [public roadmap](/cyberbaser/getting-started/roadmap/) carries the exit criteria.
- The current built product is a content-integrity and governance pipeline around an owner-controlled vault: publish boundary, projection, OFM validator, link checker, trust classifier, exact-correction primitive, a hermetically tested two-stage decision-ledger adapter, and a private owner-alpha app (bound to one owner-chosen private numeric IPv4 address, loopback by default, transport-neutral) that turns one Save into a policy-bound exact source-to-live job. Owner-alpha now has a mechanically accepted local-only Linux/amd64 OCI package with explicit rootless/rootful Docker Engine profiles. The ledger adapter is not installed in the live vault.
- The GitHub-backed dogfood path is live: 933 Markdown sources are selected and projected into 931 public page URLs through pinned Quartz, with two rehearsed contribution loops. This proves one infrastructure arrangement, not that GitHub or Quartz defines Cyberbaser.
- The account-free product path is unshipped. `OD-01` completed the full owner self-dogfood reader-to-live loop, including separate exact application, deployment, and live verification. `OD-02` and `OD-03` are Not run — superseded; their obligations moved to synthetic mechanical coverage that creates no human owner decision. This remains one maintainer's operational evidence: no ordinary-reader or independent-owner evidence exists, owner dogfood counts toward none of the deferred pilot thresholds, and Q09 remains open.
- Docs site = **the canonical KB**. Astro + Starlight publishes the research and remains parked as a product surface; `.claude/` is the fresh-agent orientation and pointer layer.

## What's Next

**Immediate milestone: select and review a WP3 runner-isolation boundary, then pass real-engine acceptance, alongside routine use of the private owner-alpha wiki.** WP1 is implemented and hermetically tested but not installed; its live-install authorization and first real observations remain a separate outward step. WP2's owner-alpha Linux/amd64 OCI image, rootless/rootful operator profiles, exact mount and private-address contract, attached bootstrap, offline Quartz seed, and readiness-aware replacement recovery are implemented and mechanically accepted. The image is local-only. The final review used a real rootless Linux daemon; an actual rootful-daemon run, physical-device Save, external-forge/live-site container Save, registry publication, and service installation remain pending. The first real acceptance Save completed on 2026-08-02 through the bare-metal route, not the container. The sequence now:

1. **Keep the product boundary honest.** The v1 product is the owner-controlled change boundary: publication selection, exact byte-preserving candidate changes, integrity classification, trust routing, and owner decision. GitHub/Actions/Pages and Quartz remain dogfood adapters and spokes.
2. ~~Build and mechanically dry-run the exact-correction path~~ **COMPLETED 2026-07-28:** `@cyberbaser/correction` resolves one exact UTF-8 quote, prepares one base-bound splice, and fails closed if the bytes changed. The internal run completed local projection, pinned rendering, link-delta comparison, redacted review output, source-isolation checks, and cleanup for one spelling-only candidate. It performed no source write or deployment and supplies no human or independent-owner evidence.
3. ~~Prepare the private operator path~~ **COMPLETED 2026-07-28:** the ignored-workspace prepare/render/decision sequence passed a complete Cyberbase-profile rehearsal, including a fresh decision-time render verification. Source bytes and Git status remained unchanged, every artifact reported zero counted evidence, and the rehearsal workspace was removed.
4. ~~Run the owner self-dogfood series~~ **CLOSED BY SUPERSESSION 2026-07-31:** `OD-01` completed the one real reader-to-live owner loop required in this phase. Preserve the immutable charter, but do not initialize `OD-02` or `OD-03`; record both as **Not run — superseded**. The `ADV-STALE-01`, `ADV-AMBIGUOUS-01`, and `ADV-REJECTION-BINDING-01` scenarios cover their safety obligations mechanically and create no human owner rejection.
5. **Use the private owner-alpha wiki for routine work.** The private app (loopback default, owner-chosen private address supported), one-Save policy, exact source operation, pre-apply checks, commit/push verification, deployment monitoring, live witness, and local Quartz rebuild are implemented. The first real acceptance Save completed under the explicitly activated durable policy on 2026-08-02; now fix only measured friction from routine use (first observed items: multi-device access, since addressed by private-address binding, and Edit-link placement). This selects an owner-local writer only; it does not select a rich external editor, hosted console, account-free intake path, or generalized adapter.
6. **Continue the next-arc architecture plan** (canonical: `docs/src/content/docs/design/v2-architecture-plan.mdx`, written and aligned 2026-08-03). **WP1 package implementation is complete:** the permissionless capture carries only a bounded routing hint; trusted Stage B binds the exact run/artifact, reconstructs authority from GitHub APIs and inert Git objects, and publishes through exact ledger-only non-force checks. The templates remain uninstalled behind a non-runnable immutable-tooling placeholder; no real fork run or live observation exists. **WP2 package implementation and mechanical acceptance are complete** within the container guide's explicit evidence boundary. **WP3 phase 1's adapter and fixture implementation are tested, but the real gate is quarantined:** the strict provider branch, dispatcher, exact run/job adapter, initializing-job polling, immediate redirect rejection, private-checkout authentication, one-time-mirrored fixture, checksum-bound runner copy, recovery coverage, workflow restrictions, 4 GiB storage guards, and cleanup fault handling exist. Adversarial review confirmed that the planned same-UID host runner can read run-root credentials and reach ambient same-user container sockets. The harness now fails before lock, run-root, or Docker creation instead of running an unsafe gate. No Forgejo image or runner was started, and the Docker data-root probe remained incomplete. Do not mark phase one complete or begin WP4 until a separately reviewed runner-isolation boundary exists and the real gate obtains complete measurements and passes. After the real gate passes, proceed to WP4 Q09 intake lanes behind `@cyberbaser/proposal`, then WP5 management console; WP6 remains open. Standing constraints remain locked.
7. **Make enforcement honest without breaking local authoring (Q10):** OFM is report-only, trust is decision-only, and the maintainer's direct push path still needs compatible validation. After a separately authorized live install, WP1's real ledger entries become the evidence input; today there are none.
8. **Close the remaining concrete gaps:** verify contributor attribution (Q08), freeze the URL contract and slug-diff gate (Q06). The general operating model question now runs through WP2–WP4.
9. **Preserve federation as long-term evidence:** keep the five-origin fixture bounded and local. Seek a concrete need from an independently operated peer before extracting any provisional profile or conformance work.

**Closed by R19 — do NOT reopen without new evidence:** the 669 remaining broken links are the maintainer's inherited content debt (old Notion exports), explicitly out of scope; the 43 emoji folder renames are declined (73 path-qualified wikilinks, and they are daily navigation). A future agent finding a pile of broken links or ugly encoded URLs should read R19 before "fixing" anything.

**Exposure triage waived (2026-07-25, maintainer decision — R11):** the vault's public content is intentional; no secrets-scan/purge remediation track. Selective publishing stays on the roadmap as the **publish-boundary feature** (the access-flexibility requirement below), a Phase 3 prerequisite rather than a safety emergency.

**New requirement captured (2026-07-02, was never in the repo):** *flexibility of access* — role-based access must be possible, and so must a simple "one private page, rest public" split despite the hierarchical KB. v1 answer is visibility (publish boundary), not identity; v2 sketch in `/cyberbaser/research/proposal-selective-publishing/`.

**Done recently:** 2026-07-02 critique recovered + landed as `/cyberbaser/research/plan-critique/` · six-agent design pass → three shapes reconciled in `/cyberbaser/research/v1-build-plan/` (proposals: hub-bom, selective-publishing, renderer-urls, write-path; adversarial: dissent, option-space) · orientation layer corrected (stale post-R08 justification, CMS-finalist mismatch, misidentified sibling project — see `.workspace/sibling-boundary-analysis.md`, private).

## Deliberately NOT Doing Right Now

- Whole-file re-serialization, direct external source writes, or a CMS/sidecar database becoming a second authority; R12 ruled those out by measurement. Rich browser or CMS-shaped presentation is not the banned part
- Selecting or building the rich trusted-contributor editor before evidence and a separate product decision justify that work
- Calling the concierge, seven-field study form, local review card, or `@cyberbaser/correction` primitive a shipped external editor, generalized adapter contract, endpoint, or account-free contribution product. The separate private owner-alpha app is an owner-local writer only
- Promoting the federation fixture into a production package, provisional profile, live Q06 URL contract, registry, shared runtime, global identity/trust system, or remote write endpoint before demonstrated peer need
- Calling controlled local fixture compatibility “interoperability” before an independently operated implementation exists
- Mixed-privacy, RBAC, real-time collaboration inside cyberbaser; assigned to the private sibling project (see `.workspace/sibling-boundary-analysis.md`)
- Extending the Astro + Starlight docs site into the product surface; it publishes the canonical KB
- Relitigating locked decisions above without new evidence

## Running the Docs Site

```bash
cd docs
bun run dev          # http://localhost:4321/cyberbaser/ (dev, HMR — may be flaky on mobile)
bun run dev:host     # bind to 0.0.0.0 (Tailscale / LAN)
bun run build        # production build
bun run preview --host 0.0.0.0  # serve built output (use this for mobile testing via Tailscale)
bun run test:local   # headless Playwright suites (layout + smoke; test:e2e is --headed)
```

## Pointers to External Context

- **Live content vault**: https://github.com/cybersader/cyberbase
- **Local research vault**: Obsidian vault (local path varies by machine)
- **Identity/essence + risks**: `/cyberbaser/research/assumptions-and-risks/` · SSOT findings: `/cyberbaser/research/source-of-truth/` · v1 stack: `/cyberbaser/research/v1-architecture/`
- **Self-hosted auth**: `/cyberbaser/design/reference-architectures/self-hosted-forgejo-auth/` (RA-01)
- **Challenge briefs**: `/cyberbaser/agent-context/zz-challenges/` · **Roadmap**: `/cyberbaser/getting-started/roadmap/`
- **Sibling repos**: `cybersader/crosswalker`, `cybersader/cyberchaste`, a private browser-Obsidian sibling (boundary analysis: `.workspace/sibling-boundary-analysis.md`, not for the public repo), Sinario (cyber scenarios)
